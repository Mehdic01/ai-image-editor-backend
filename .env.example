# Example environment configuration for AI Image Editor Backend
# Copy this file to `.env` and replace placeholder values as needed.

# Database (SQLite for local dev; use Postgres in production)
DATABASE_URL=sqlite:///./app.db

# CORS: comma-separated allowed origins for your frontend(s)
# For local development you can include common ports for React/Vite/Flutter web
ALLOWED_ORIGINS=http://127.0.0.1:3000,http://localhost:3000,http://127.0.0.1:5173,http://localhost:5173,http://127.0.0.1:8000,http://localhost:8000

# fal.ai credentials and model configuration
# Get an API key from https://fal.ai
FAL_API_KEY=YOUR_FAL_API_KEY

# Default model used by the backend
FAL_MODEL=fal-ai/seedream-v4
# Alternative edit model example:
# FAL_MODEL=fal-ai/bytedance/seedream/v4/edit

# fal.ai endpoint (usually https://fal.run)
FAL_ENDPOINT=https://fal.run

# Optional: request timeouts and polling behavior (seconds)
# FAL_TIMEOUT=120
# POLL_INTERVAL=1.5
# POLL_MAX_WAIT=120

# Optional: dev convenience CORS regex to allow any localhost:* origin
# Only enable if you wire allow_origin_regex in app/main.py
# ALLOWED_ORIGIN_REGEX=^https?://(localhost|127\.0\.0\.1)(:\d+)?$

# Storage backend: "local" (default) or "s3" for AWS S3 / R2 / any S3-compatible storage
# STORAGE_BACKEND=local
# S3_BUCKET=your-bucket
# S3_REGION=eu-central-1
# S3_ENDPOINT=https://<accountid>.r2.cloudflarestorage.com
# S3_ACCESS_KEY_ID=your-access-key
# S3_SECRET_ACCESS_KEY=your-secret
# S3_PUBLIC_BASE_URL=https://cdn.example.com
